let gif_hotdog;

function preload() {
    gif_hotdog = createImg('assets/hotdog.gif', 500, 500);
}

function setup() {
    //createCanvas(500, 700);
}

function draw() {
    //background(0);
    gif_hotdog.center();
}